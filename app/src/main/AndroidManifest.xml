<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
          xmlns:dist="http://schemas.android.com/apk/distribution"
          package="com.example.lol.biblioteca">

    <dist:module dist:instant="true"/>

    <application
            android:allowBackup="true"
            android:icon="@mipmap/ic_launcher"
            android:label="@string/app_name"
            android:roundIcon="@mipmap/ic_launcher_round"
            android:supportsRtl="true"
            android:theme="@style/Theme.AppCompat.Light.NoActionBar">
     <activity
                android:name=".activity.activity.CadastrarLivro"
                android:label="@string/title_activity_categorias_livros"
                android:theme="@style/NoActionBar">
        </activity>
        <!-- O "NoActionBar faz com que o sistema não utilize o actionbar padrão -->
        <activity android:name=".activity.activity.MainActivity">
            <intent-filter>
                <action android:name="android.intent.action.MAIN"/>

                <category android:name="android.intent.category.LAUNCHER"/>
            </intent-filter>
        </activity>
        <activity android:name=".activity.activity.Cadastro"/>
        <activity android:name=".activity.activity.Livros"/>
    </application>

</manifest>